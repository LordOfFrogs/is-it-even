function isEven(number: number) {
    const request = new Request("https://api.isevenapi.xyz/api/${number}/");
    
}

export default function Home() {
    return (
        <main>
            
        </main>
    );
}